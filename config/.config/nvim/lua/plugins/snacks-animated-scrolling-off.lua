return {
  "folke/snacks.nvim",
  opts = {
    scroll = {
      enabled = false, -- Disable scrolling animations
    },
    explorer = {
      layout = { layout = { position = "right" } },
      git_status = true,
      git_untracked = true,
      follow_file = true,
      hidden = true,
    },
  },
}
