#!/bin/bash

# Omarchy relaunches swaybg whenever the theme changes, which would cover up
# the shader wallpaper. Re-run the launcher so the new theme's background gets
# picked up as the shader's base image instead.

"$HOME/.local/bin/shader-wallpaper"
